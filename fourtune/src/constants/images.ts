import noImage from '../assets/images/no-image.png';
import logo from '../assets/images/logo.png';

// 경매 기본 이미지 (이미지가 없을 경우 사용)
export const DEFAULT_AUCTION_IMAGE = noImage;

// 서비스 로고 이미지
export const LOGO_IMAGE = logo;
